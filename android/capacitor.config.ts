import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.escritorio.rf',
  appName: 'Escritório RF',
  webDir: 'https://malaca7.github.io/rfdrive/#/admin/'
};

export default config;
